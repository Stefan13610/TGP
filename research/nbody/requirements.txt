# TGP N-body package — Python dependencies
# Tested with Python 3.10, 3.12 (CI) and 3.14.2 (dev, Windows 11)
# Install: pip install -r requirements.txt
#
# For deterministic reproduction use the pinned lockfile:
#   pip install -r requirements.lock  (in repo root)

numpy>=1.26
scipy>=1.12
matplotlib>=3.8
